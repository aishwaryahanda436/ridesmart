package com.ridesmart.ui.theme

import androidx.compose.ui.graphics.Color

// ── Neutral backgrounds ──────────────────────────────
val BgDeep      = Color(0xFF0B0B0F)   // main screen bg
val BgSurface   = Color(0xFF14141A)   // card bg
val BgElevated  = Color(0xFF1C1C24)   // input fields, elevated cards

// ── Primary accent: Amber (timer bar, live requests) ─
val Amber400    = Color(0xFFEF9F27)
val Amber200    = Color(0xFFFAC775)
val Amber800    = Color(0xFF633806)
val AmberLight  = Color(0xFFFAEEDA)

// ── Accept: Green ────────────────────────────────────
val Green400    = Color(0xFF639922)
val Green200    = Color(0xFF97C459)
val GreenLight  = Color(0xFFEAF3DE)
val GreenDark   = Color(0xFF27500A)

// ── Skip: Red ────────────────────────────────────────
val Red400      = Color(0xFFE24B4A)
val RedLight    = Color(0xFFFCEBEB)
val RedDark     = Color(0xFF791F1F)

// ── Borderline: Yellow ───────────────────────────────
val Yellow400   = Color(0xFFCA8A04)

// ── Text ─────────────────────────────────────────────
val TextPrimary   = Color(0xFFF0EEE8)
val TextSecondary = Color(0xFF8A8880)
val TextMuted     = Color(0xFF50504C)

// ── Borders ──────────────────────────────────────────
val BorderSubtle  = Color(0xFF242430)
val BorderDefault = Color(0xFF303040)
// ── Amber (primary – main actions / highlights) ──
val Amber400  = Color(0xFFFFCA28)
val AmberLight = Color(0xFFFFE082)
val Amber800  = Color(0xFFFF8F00)

// ── Green (secondary – positive / go signals) ──
val Green400   = Color(0xFF66BB6A)
val GreenLight = Color(0xFFA5D6A7)
val GreenDark  = Color(0xFF2E7D32)

// ── Red (tertiary / error – stop / alert signals) ──
val Red400 = Color(0xFFEF5350)

// ── Backgrounds ──
val BgDeep     = Color(0xFF0F1117)
val BgSurface  = Color(0xFF1A1C24)
val BgElevated = Color(0xFF252830)

// ── Text ──
val TextPrimary   = Color(0xFFE8EAED)
val TextSecondary = Color(0xFF9AA0A6)

// ── Borders ──
val BorderDefault = Color(0xFF3C4043)
val BorderSubtle  = Color(0xFF282A2D)
